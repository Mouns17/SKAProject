<Window
    x:Class="SKAProject.AddPostWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SKAProject"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="420"
    Height="280"
    AllowsTransparency="True"
    Background="Transparent"
    FontFamily="Arian"
    ResizeMode="NoResize"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    WindowState="Normal"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid MouseLeftButtonDown="MainBorder_MouseLeftButtonDown">
        <Border
            Width="420"
            Height="280"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="#EDF4FF"
            BorderBrush="#6366F1"
            BorderThickness="1"
            CornerRadius="20">
            <Grid>
                <StackPanel>
                    <Button
                        Margin="375,5,0,80"
                        Click="BtnClose"
                        Content="✖︎"
                        Style="{StaticResource ControlBtnStyle}" />

                    <TextBlock
                        Margin="0,-60,0,0"
                        HorizontalAlignment="Center"
                        FontSize="28"
                        FontWeight="Bold"
                        Text="Создание должности" />

                    <TextBlock
                        Margin="0,-25,0,0"
                        FontSize="12"
                        Foreground="#6366F1"
                        Text="Чтобы создать новую должность, заполните все поля"
                        TextAlignment="Center" />

                    <Border
                        Height="1"
                        Margin="40,0,40,0"
                        Background="{StaticResource DividerBrush}" />

                    <TextBox
                        x:Name="TBoxPostName"
                        Width="300"
                        Height="35"
                        Margin="0,15,0,15"
                        HorizontalAlignment="Center"
                        Style="{StaticResource TxtBoxStyle}"
                        Tag="Введите название должности"
                        Visibility="Visible" />

                    <Border
                        Height="1"
                        Margin="40,0,40,0"
                        Background="{StaticResource DividerBrush}" />

                    <Button
                        x:Name="BtnAddVis"
                        Margin="50,15,50,0"
                        Click="BtnAdd"
                        Content="Добавить"
                        Style="{StaticResource LogBtnStyle}"
                        Visibility="Visible" />

                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
