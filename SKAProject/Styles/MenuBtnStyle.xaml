<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="MenuBtnStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextForeground}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FontFamily" Value="Arian" />
        <Setter Property="FontSize" Value="25" />
        <Setter Property="Height" Value="48" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="menuButton"
                        Background="{TemplateBinding Background}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="25" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <Border
                                x:Name="btnSelected"
                                Grid.ColumnSpan="2"
                                Width="315"
                                HorizontalAlignment="Right"
                                CornerRadius="10" />

                            <Rectangle
                                Name="Indicator"
                                Width="6"
                                Height="30"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                RadiusX="3"
                                RadiusY="3">
                                <Rectangle.BitmapEffect>
                                    <DropShadowBitmapEffect
                                        Direction="0"
                                        ShadowDepth="3"
                                        Softness="0.6"
                                        Color="#6366F1" />
                                </Rectangle.BitmapEffect>
                            </Rectangle>

                            <TextBlock
                                x:Name="txtName"
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="{TemplateBinding FontSize}"
                                FontWeight="{TemplateBinding FontWeight}"
                                Foreground="{TemplateBinding Foreground}"
                                Text="{TemplateBinding Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="txtName" Property="Foreground" Value="{DynamicResource MenuButtonHoverForeground}" />
                            <Setter TargetName="btnSelected" Property="Background" Value="{DynamicResource MenuButtonSelectedBackground}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Indicator" Property="Fill" Value="{DynamicResource MenuButtonIndicatorFill}" />
                            <Setter TargetName="btnSelected" Property="Background" Value="{DynamicResource MenuButtonSelectedBackground}" />
                            <Setter TargetName="txtName" Property="Foreground" Value="{DynamicResource MenuButtonHoverForeground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>