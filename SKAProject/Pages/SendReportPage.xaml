<Page x:Class="SKAProject.Pages.SendReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Отправить отчёт"
      Background="{DynamicResource ContentBackgroundBrush}"
      FontFamily="Arian">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Отправка отчёта" FontSize="36" FontWeight="SemiBold"
                   Foreground="{DynamicResource ForegroundColor1}" Margin="0,0,0,20"/>
        <TextBlock Grid.Row="1" Text="Заполните форму для отправки отчёта руководителю"
                   FontSize="22" Foreground="{DynamicResource ForegroundColor1}" Margin="0,0,0,25"/>

        <Border Grid.Row="2" Background="{DynamicResource BackgroundColor3}" CornerRadius="20"
                Padding="25" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.12" ShadowDepth="0" Color="#000000"/>
            </Border.Effect>
            <StackPanel>
                <!-- Получатель -->
                <TextBlock Text="Кому:" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundColor1}" Margin="0,0,0,5"/>
                <ComboBox x:Name="CmbRecipient" Height="45" FontSize="18"
                          Background="{DynamicResource BackgroundColor3}"
                          Foreground="{DynamicResource ForegroundColor1}"
                          DisplayMemberPath="FullName" SelectedValuePath="UserId"
                          Margin="0,0,0,15"/>

                <!-- Дата -->
                <TextBlock Text="Дата отчёта:" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundColor1}" Margin="0,0,0,5"/>
                <DatePicker x:Name="DpReportDate" Height="45" FontSize="18"
                            Margin="0,0,0,15"/>

                <!-- Описание -->
                <TextBlock Text="Описание:" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundColor1}" Margin="0,0,0,5"/>
                <TextBox x:Name="TxtDescription" Style="{DynamicResource TxtBoxStyle}"
                         Height="100" FontSize="18" TextWrapping="Wrap" AcceptsReturn="True"
                         Margin="0,0,0,15"/>

                <!-- Файл -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <Button x:Name="BtnAttach" Content="📎 Прикрепить файл"
                            Background="{DynamicResource BackgroundColor6}" Foreground="White"
                            FontSize="16" FontWeight="SemiBold" Height="40" Width="200"
                            BorderThickness="0" Cursor="Hand" Click="BtnAttach_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <TextBlock x:Name="TxtAttachedFile" FontSize="16" Foreground="{DynamicResource ForegroundColor1}"
                               VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>

                <Button x:Name="BtnSend" Content="Отправить отчёт" Height="45" Width="200"
                        Background="{DynamicResource BackgroundColor6}" Foreground="White"
                        FontSize="16" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"
                        Click="BtnSend_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>