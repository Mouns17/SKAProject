<Page
    x:Class="SKAProject.Pages.OrdersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Приказы"
    Background="{DynamicResource ContentBackgroundBrush}"
    FontFamily="Arian">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,20"
            FontSize="36"
            FontWeight="SemiBold"
            Foreground="{DynamicResource ForegroundColor1}"
            Text="Приказы" />
        <TextBlock
            Grid.Row="1"
            Margin="0,0,0,25"
            FontSize="22"
            Foreground="{DynamicResource ForegroundColor1}"
            Text="Управление кадровыми приказами" />

        <StackPanel
            Grid.Row="2"
            Margin="0,0,0,15"
            Orientation="Horizontal">
            <Button
                x:Name="BtnAdd"
                Width="220"
                Height="45"
                Background="{DynamicResource BackgroundColor6}"
                BorderThickness="0"
                Click="BtnAdd_Click"
                Content="+ Создать приказ"
                Cursor="Hand"
                FontSize="16"
                FontWeight="SemiBold"
                Foreground="White">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <ComboBox
                x:Name="CmbEmployee"
                Width="300"
                Height="45"
                Margin="15,0,0,0"
                VerticalContentAlignment="Center"
                Background="{DynamicResource BackgroundColor3}"
                DisplayMemberPath="FullName"
                FontSize="16"
                Foreground="{DynamicResource ForegroundColor1}"
                SelectedValuePath="UserId">
                <ComboBox.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.5"
                        ShadowDepth="0"
                        Color="#010210" />
                </ComboBox.Effect>
            </ComboBox>
        </StackPanel>

        <!--  Таблица приказов (без изменений)  -->
        <Border
            Grid.Row="3"
            Background="{DynamicResource BackgroundColor3}"
            CornerRadius="20">
            <!--  ... ваш существующий код таблицы ...  -->
        </Border>
    </Grid>
</Page>