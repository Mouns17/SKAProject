<Window
    x:Class="SKAProject.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SKAProject"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="1920"
    Height="1080"
    Background="#b7b4e0"
    FontFamily="Arian"
    ResizeMode="CanResize"
    Topmost="False"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid MouseLeftButtonDown="MainBorder_MouseLeftButtonDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" />
            <ColumnDefinition Width="1560" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="360,0,-1560,0">
            <Button
                Margin="1320,10,0,0"
                Click="BtnRollup"
                Content="➖"
                Foreground="#010210"
                Style="{StaticResource ControlBtnStyle2}" />

            <Button
                Margin="1405,-35,0,0"
                Click="BtnMinimize"
                Content="🔲"
                Foreground="#010210"
                Style="{StaticResource ControlBtnStyle2}" />

            <Button
                Margin="1490,-35,0,80"
                Click="BtnClose"
                Content="✖︎"
                Foreground="#010210"
                Style="{StaticResource ControlBtnStyle2}" />

            <Border
                Height="40"
                Margin="15,-120,150,70"
                Background="#EDF4FF"
                BorderBrush="{StaticResource BackgroundColor6}"
                BorderThickness="1"
                CornerRadius="15">
                <DockPanel Margin="0,0,0,0">
                    <TextBlock
                        Margin="5,10,0,0"
                        DockPanel.Dock="Left"
                        FontSize="14"
                        Foreground="#666666"
                        Text="🔍" />
                    <TextBox
                        x:Name="SearchBox2"
                        Margin="8,0,0,0"
                        VerticalContentAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="13"
                        Foreground="#010210"
                        TextChanged="SearchBox2_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Grid>
                                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
                                                <TextBlock
                                                    x:Name="Placeholder"
                                                    VerticalAlignment="Center"
                                                    Foreground="#666666"
                                                    IsHitTestVisible="False"
                                                    Text="Поиск..."
                                                    Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </DockPanel>
            </Border>
        </StackPanel>

        <!--  Боковая менюшка(где кнопки типа)  -->
        <Border
            Grid.RowSpan="2"
            Grid.Column="0"
            Background="{StaticResource BackgroundColor4}"
            BorderBrush="{StaticResource BackgroundColor6}"
            BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20,20,20">

                <Image
                    Width="115"
                    Height="80"
                    Margin="0,0,170,0"
                    Source="\Images\Logotype.png" />

                <TextBlock
                    Width="100"
                    Height="100"
                    Margin="60,-80,0,0"
                    FontFamily="Arian Bold"
                    FontSize="40"
                    Foreground="Black"
                    Text="СКА" />

                <TextBlock
                    Width="187"
                    Height="100"
                    Margin="153,-58,0,0"
                    FontFamily="Arian Bold"
                    FontSize="17"
                    Foreground="Black"
                    Text="СИСТЕМА КОНТРОЛЯ И АНАЛИЗА"
                    TextWrapping="Wrap" />

                <Button
                    Margin="0,-40,0,0"
                    Click="Home_Click"
                    Content="🏠  Главное меню"
                    Style="{StaticResource MenuBtnStyle}" />

                <Button
                    Margin="0,15,0,0"
                    Click="Profile_Click"
                    Content="👤  Профиль"
                    Style="{StaticResource MenuBtnStyle}" />

                <Button
                    x:Name="BtnWorkers"
                    Margin="0,15,0,0"
                    Click="Workers_Click"
                    Content="👥  Список работников"
                    Style="{StaticResource MenuBtnStyle}"
                    Visibility="Visible" />

                <Button
                    x:Name="BtnOrganization"
                    Margin="0,15,0,0"
                    Click="MyOrg_Click"
                    Content="🏢  Моя организация"
                    Style="{StaticResource MenuBtnStyle}"
                    Visibility="Visible" />

                <Button
                    Margin="0,15,0,0"
                    Click="MyTasks_Click"
                    Content="📅  Мои задачи"
                    Style="{StaticResource MenuBtnStyle}" />

                <Button
                    x:Name="BtnReports"
                    Margin="0,15,0,0"
                    Click="Otcheti_Click"
                    Content="📂  Отчеты работников"
                    Style="{StaticResource MenuBtnStyle}"
                    Visibility="Visible" />

                <Button
                    x:Name="BtnLogs"
                    Margin="0,15,0,0"
                    Click="Logs_Click"
                    Content="🗂  Логи"
                    Style="{StaticResource MenuBtnStyle}"
                    Visibility="Visible" />

                <Button
                    Margin="0,380"
                    Click="Logout_Click"
                    Content="🚪  Выход"
                    Style="{StaticResource MenuBtnStyle}" />
            </StackPanel>
        </Border>

        <Frame
            x:Name="MainFrame"
            Grid.RowSpan="2"
            Grid.Column="1"
            Navigated="MainFrame_Navigated"
            NavigationUIVisibility="Hidden" />

    </Grid>
</Window>