<Window x:Class="SKAProject.MainWindow" WindowStyle="None" WindowState="Maximized" Topmost="False" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Background="#b7b4e0"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SKAProject"
        mc:Ignorable="d"
        Height="1080" Width="1920">

    <Window.Resources>
        <SolidColorBrush x:Key="SideBar" Color="#EDF4FF"/>
        <SolidColorBrush x:Key="Hover" Color="#b7b4e0"/>
        <SolidColorBrush x:Key="Accent" Color="#b7b4e0"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#370066"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style x:Key="MenuBtnStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#010002"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontFamily" Value="Palqa"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="menuButton"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="25"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <!-- Selected  -->
                                <Border x:Name="btnSelected"
                                        Grid.ColumnSpan="2"
                                        CornerRadius="10"
                                        Width="315" HorizontalAlignment="Right">
                                </Border>

                                <!-- Indicator -->
                                <Rectangle Name="Indicator"
                                           HorizontalAlignment="Left"
                                           Width="6" Height="30"
                                           VerticalAlignment="Center"
                                           RadiusX="3" RadiusY="3">
                                    <Rectangle.BitmapEffect>
                                        <DropShadowBitmapEffect
                                            ShadowDepth="3"
                                            Direction="0"
                                            Color="#1c0033"
                                            Softness="0.6"/>
                                    </Rectangle.BitmapEffect>
                                </Rectangle>

                                <!-- Text -->
                                <TextBlock x:Name="txtName" HorizontalAlignment="Left"
                                           VerticalAlignment="Center" Margin="12 0 0 0"
                                           Grid.Column="1" Text="{TemplateBinding Content}"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontWeight="{TemplateBinding FontWeight}"
                                           FontSize="{TemplateBinding FontSize}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="txtName" Property="Foreground" Value="#090011"/>
                                <Setter TargetName="btnSelected" Property="Background" Value="#b7b4e0"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Indicator" Property="Fill" Value="#250044"/>
                                <Setter TargetName="btnSelected" Property="Background" Value="#b7b4e0"/>
                                <Setter TargetName="txtName" Property="Foreground" Value="#250044"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>



        <!-- Стиль для кнопкок (боковых - не названий разделов) -->
        <Style x:Key="MenuBtn" TargetType="Button">
            <Setter Property="Height" Value="52"/>
            <Setter Property="FontFamily" Value="Palqa"/>
            <Setter Property="Margin" Value="10 4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#010002"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="18 0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <!-- Тригеры для курсора -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource Hover}"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource Accent}"/>
                </Trigger>
            </Style.Triggers>

            <!-- Стиль для боковых названий разделов -->
        </Style>
        <Style TargetType="TextBlock" x:Key="MenuHeader">
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="20 0 20 20"/>
        </Style>
    </Window.Resources>
    <!-- Основная сетка -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="17*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360"/>
            <ColumnDefinition Width="1560"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="360 0 -1560 0">
            <Button Content="➖" Style="{StaticResource ControlBtnStyle}" Margin="1450,5,0,0" Click="BtnRollup"/>

            <Button Content="✖︎" Style="{StaticResource ControlBtnStyle}" Margin="1510, -25 0,80" Click="BtnClose"/>
        </StackPanel>
        
        
        <!-- Боковая менюшка(где кнопки типа) -->
        <Border Background="{StaticResource SideBar}" Grid.Column="0" Grid.RowSpan="2">
            <StackPanel Margin="0 20 20 20" >
                <TextBlock Text="Здесь потом будет логотип" FontSize="25" Foreground="#09030e" Margin="20 0 50 0" Width="320" Height="100"/>

                <Button Style="{StaticResource MenuBtnStyle}" Content="🏠  Главное меню" Click="Home_Click" Margin="0 0 0 0"/>

                <Button Style="{StaticResource MenuBtnStyle}" Content="👤  Профиль" Click="Profile_Click" Margin="0 15 0 0"/>

                <Button Style="{StaticResource MenuBtnStyle}" Content="🔐  Аккаунт" Click="Account_Click" Visibility="Collapsed" Margin="0 15 0 0"/>

                <Button Style="{StaticResource MenuBtnStyle}" Content="⚙  Настройки" Click="Settings_Click" Margin="0 15 0 0"/>

                <Button Style="{StaticResource MenuBtnStyle}" Content="💾  Экспорт данных" Click="Export_Click" Margin="0 15 0 0"/>

                <Button Style="{StaticResource MenuBtnStyle}" Content="🗂  Логи" Click="Logs_Click" Visibility="Collapsed" Margin="0 15 0 0"/>

                <Border CornerRadius="25" Background="#250044" Margin="30 400 30 0" Height="140" Width="260">

                </Border>

                <Button Style="{StaticResource MenuBtnStyle}" Content="🚪  Выход" Click="Logout_Click" Margin="0 80"/>
            </StackPanel>
        </Border>

        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden" Grid.RowSpan="2"/>

    </Grid>
</Window>