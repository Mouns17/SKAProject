<Application x:Class="SKAProject.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SKAProject"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>
        <!-- Стиль для TextBox в виде плашки с изменением обводки при фокусе типа -->
            <Style x:Key="TxtBoxStyle" TargetType="TextBox">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="#250044"/>
                <Setter Property="BorderThickness" Value="1.4"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Grid>
                                <Border x:Name="Border" CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="10 0"/>
                                <TextBlock x:Name="hint" Text="{TemplateBinding Tag}" Foreground="DimGray" Margin="22 0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#513965"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter TargetName="hint" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Стиль для кнопки перехода далее -->
            <Style x:Key="LogBtnStyle" TargetType="Button">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="#250044"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Height" Value="42"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border" CornerRadius="12" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#FFFFFF"/>
                                </Trigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#370066"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Стиль для кнопки перехода в окно регистрации -->
        <Style x:Key="RegBtnStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1.4"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" CornerRadius="12" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="DimGray"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="WhiteSmoke"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кнопок закрыть и свернуть окно -->
        <Style x:Key="ControlBtnStyle" TargetType="Button">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Opacity" Value="10"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1.4"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="25"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Разделительная линия -->
        <LinearGradientBrush x:Key="DividerBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#00250044" Offset="0"/>
            <GradientStop Color="#FF250044" Offset="0.5"/>
            <GradientStop Color="#FF2e0055" Offset="0.7"/>
            <GradientStop Color="#002e0055" Offset="1"/>
        </LinearGradientBrush>

        <!-- Background Colors -->
        <SolidColorBrush x:Key="PrimaryBackgroundColor" Color="#C0C9EA"/>
        <SolidColorBrush x:Key="SecundaryBackgroundColor" Color="#EDF4FF"/>
        <SolidColorBrush x:Key="TertiaryBackgroundColor" Color="#FFFFFF"/>

        <!-- System Colors -->
        <!-- Typograhy Colors -->
        <SolidColorBrush x:Key="PrimaryTextColor" Color="#0E1A32"/>
        <SolidColorBrush x:Key="SecundaryTextColor" Color="#102341"/>
        <SolidColorBrush x:Key="TertiaryTextColor" Color="#1C3254"/>

        <!-- Blue Colors-->
        <SolidColorBrush x:Key="PrimaryBlueColor" Color="#004FC4"/>
        <SolidColorBrush x:Key="SecundaryBlueColor" Color="#0063F7"/>
        <SolidColorBrush x:Key="TertiaryBlueColor" Color="#5B8DEF"/>

        <!-- Red Colors-->
        <SolidColorBrush x:Key="PrimaryRedColor" Color="#E63535"/>
        <SolidColorBrush x:Key="SecundaryRedColor" Color="#FF3B3B"/>
        <SolidColorBrush x:Key="TertiaryRedColor" Color="#FF5C5C"/>

        <!-- Green Colors-->
        <SolidColorBrush x:Key="PrimaryGreenColor" Color="#05A660"/>
        <SolidColorBrush x:Key="SecundaryGreenColor" Color="#06C270"/>
        <SolidColorBrush x:Key="TertiaryGreenColor" Color="#39D98A"/>

        <!-- Orange Colors-->
        <SolidColorBrush x:Key="PrimaryOrangeColor" Color="#E67A00"/>
        <SolidColorBrush x:Key="SecundaryOrangeColor" Color="#FF8800"/>
        <SolidColorBrush x:Key="TertiaryOrangeColor" Color="#FDAC42"/>

        <!-- Yellow Colors-->
        <SolidColorBrush x:Key="PrimaryYellowColor" Color="#E6B800"/>
        <SolidColorBrush x:Key="SecundaryYellowColor" Color="#FFCC00"/>
        <SolidColorBrush x:Key="TertiaryYellowColor" Color="#FDDD48"/>

        <!-- Teal Colors-->
        <SolidColorBrush x:Key="PrimaryTealColor" Color="#00B7C4"/>
        <SolidColorBrush x:Key="SecundaryTealColor" Color="#00CFDE"/>
        <SolidColorBrush x:Key="TertiaryTealColor" Color="#73DFE7"/>

        <!-- White -->
        <SolidColorBrush x:Key="PrimaryWhiteColor" Color="#C0C9EA"/>
        <SolidColorBrush x:Key="SecundaryWhiteColor" Color="#EDF4FF"/>
        <SolidColorBrush x:Key="TertiaryWhiteColor" Color="#FFFFFF"/>

        <!-- Black -->
        <SolidColorBrush x:Key="PrimaryBlackColor" Color="#0E1A32"/>
        <SolidColorBrush x:Key="SecundaryBlackColor" Color="#102341"/>
        <SolidColorBrush x:Key="TertiaryBlackColor" Color="#1C3254"/>

    </Application.Resources>
</Application>
